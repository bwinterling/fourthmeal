<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content=" width=device-width, initial-scale=1, minimum-scale=1, maximum-scale=1" />
  <title>
    OnoBurrito
    <% if content_for?(:title) %>
       - <%= yield :title %>
    <% end %>
  </title>
  <%= stylesheet_link_tag "application", media: "all"%> 
  <%= javascript_include_tag "application"%>
  <%= csrf_meta_tags %>
  <link href='http://fonts.googleapis.com/css?family=Sintony' rel='stylesheet' type='text/css'>
  <link href='http://fonts.googleapis.com/css?family=Podkova:700' rel='stylesheet' type='text/css'>
</head>  

<body>
  <div id="wrapper">
    <header class="grid-container" id="page_header" role="header">
      <%= render 'layouts/header' %>
    </header>

    <nav class="grid-container" id="main-nav-container" role="navigation">
      <%= render "layouts/main_nav" %>
    </nav>

    <main role="main">
      <div class="grid-container" id="flash-section">
        <% flash.each do |name, msg| %>
          <%= content_tag :div, msg, :id => "flash_#{name}" %>
        <% end %>
      </div>
      <%= yield %>
    </main>

    <footer class="grid-container" id="footer-container" role="footer">
      <%= render 'layouts/footer' %>
    </footer>
    
  </div>
</body>

</html>
